import { YStack, styled } from 'tamagui';

export const PageWrapper = styled(YStack, {
  width: '100%',
  height: '100vh',
  overflow: 'scroll',
  padding: 20,
  paddingHorizontal: 60,
  space: '$4',
});
