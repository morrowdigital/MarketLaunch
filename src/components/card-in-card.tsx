import { styled } from 'tamagui';
import { ElevatedCard } from '~/components/elevated-card';

export const CardInCard = styled(ElevatedCard, {
  shadowColor: 'rgba(0,0,0,0.5)',
  borderRadius: 0,
  width: '100%',
});
